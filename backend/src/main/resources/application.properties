server.port=${PORT:3000}
server.address=0.0.0.0

spring.datasource.hikari.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.jdbc-url=jdbc:mysql://sakura.proxy.rlwy.net:35225/railway?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.hikari.username=${MYSQLUSER}
spring.datasource.hikari.password=${MYSQLPASSWORD}

jwt.secret=${JWT_SECRET}

# Swagger Error
spring.mvc.pathmatch.matching-strategy=ant_path_matcher